name: Deploy Bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Restart services
        run: |
          cd ~/Projects/archipelago_notifier
          git pull origin main
          sudo systemctl restart archipelago_notifier.service